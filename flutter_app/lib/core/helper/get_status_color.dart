// import 'package:flutter/material.dart';

// import '../enums/request_status.dart';
// import '../styles/app_colors.dart';

// Color getStatusBackgroundForRequests(
//   RequestStatus? status,
//   BuildContext context,
// ) {
//   switch (status) {
//     case RequestStatus.approved:
//       return context.primaryColor.withValues(alpha: 0.12);
//     case RequestStatus.rejected:
//       return context.redColor.withValues(alpha: 0.08);
//     case RequestStatus.pending:
//       return context.pendingColor.withValues(alpha: 0.1);
//     case RequestStatus.completed:
//       return context.completedColor.withValues(alpha: 0.12);
//     case null:
//       return context.errorColor.withValues(alpha: 0.35);
//   }
// }

// Color getStatusColorForRequests(RequestStatus? status, BuildContext context) {
//   switch (status) {
//     case RequestStatus.approved:
//       return context.primaryColor;
//     case RequestStatus.rejected:
//       return context.redColor;
//     case RequestStatus.pending:
//       return context.pendingColor;
//     case RequestStatus.completed:
//       return context.completedColor;
//     case null:
//       return context.errorColor;
//   }
// }
